# ========================================
# LDAP Configuration
# ========================================
# LDAP server URL
# Examples: 
#   - ldap://localhost:389 (unencrypted)
#   - ldaps://ldap.example.com:636 (SSL/TLS)
LDAP_SERVER=ldap://localhost:389
# DN to use for binding to LDAP
# This account needs read access to groups and user objects
LDAP_BIND_DN=cn=admin,dc=example,dc=com
# Password for the bind DN
LDAP_BIND_PASSWORD=admin_password
# Base DN where groups are located
# The script will search for groups under this DN
LDAP_GROUP_BASE_DN=ou=groups,dc=example,dc=com
# LDAP filter to find groups
# Common values:
#   - (objectClass=groupOfNames) - standard LDAP groups
#   - (objectClass=posixGroup) - POSIX groups
#   - (objectClass=group) - Active Directory groups
LDAP_GROUP_FILTER=(objectClass=groupOfNames)
# Attribute that contains group members
# Common values:
#   - member (for groupOfNames)
#   - memberUid (for posixGroup)
#   - uniqueMember (for groupOfUniqueNames)
LDAP_MEMBER_ATTRIBUTE=member
# Use TLS (StartTLS) for LDAP connection
# Set to true if using ldap:// with StartTLS
# Not needed if using ldaps://
LDAP_USE_TLS=false
# Path to custom CA certificate file for LDAP TLS verification (optional)
# Use this if your LDAP server uses a self-signed certificate or internal CA
# Example: /path/to/ca-cert.pem
LDAP_CA_CERT_FILE=
# ========================================
# OpenFGA Configuration
# ========================================
# OpenFGA API URL
# Example: http://localhost:8080 or https://api.fga.example.com
OPENFGA_API_URL=http://localhost:8080
# Your OpenFGA store ID
# Get this from: fga store list
OPENFGA_STORE_ID=your-store-id-here
# API token for authentication (optional)
# Leave empty if your OpenFGA instance doesn't require auth
OPENFGA_API_TOKEN=
# Authorization model ID (optional)
# Leave empty to use the latest model
OPENFGA_AUTHORIZATION_MODEL_ID=
# ========================================
# Sync Configuration
# ========================================
# Comma-separated list of groups to sync
# Only these groups will be synced from LDAP to OpenFGA
# Example: developers,operations,managers
SYNC_GROUPS=
# Dry run mode
# Set to true to preview changes without applying them
# Set to false to actually sync memberships
SYNC_DRY_RUN=false
